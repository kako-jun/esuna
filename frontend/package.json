{
  "name": "esuna-frontend",
  "version": "0.6.0",
  "description": "Esuna - 視覚障害者向けアクセシブルWebアプリケーション",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node -e \"const f=require('fs'),p='dist/sw.js';f.writeFileSync(p,f.readFileSync(p,'utf-8').replace('__BUILD_DATE__',new Date(Date.now()+9*36e5).toISOString().split('T')[0]))\"",
    "preview": "vite preview"
  },
  "dependencies": {
    "solid-js": "^1.9.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vite-plugin-solid": "^2.11.0"
  }
}
